# Sample Board Workspace

Open this folder from AI Kanban to inspect the portable folder contract without
changing the sample. Duplicate the folder before experimenting. `board.sqlite`
is canonical; `manifest.json` declares owned paths and fingerprints; the root
instructions point to the single agent skill bundle.

AI Kanban does not modify this README or other unrecognized files.
