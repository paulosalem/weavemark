# Terminology guidance

Call an individual reusable WeaveMark artifact a promplet. Distinguish compilation,
which produces prompt artifacts, from interpretation, which runs them through an
execution engine.
