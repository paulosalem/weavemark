# Project overview

The release adds source references that can inform compilation and, when retained,
appear in a deterministic Reference Appendix.

Inline references produce stable anchors. References inside Markdown code spans
and fenced code remain literal.
