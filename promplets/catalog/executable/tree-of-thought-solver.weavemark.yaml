# Tree of Thought Solver — Runtime Config

engine: tree-of-thought

engine_config:
  branching_factor: 3
  max_depth: 2

prompts:
  generate:
    model: gpt-5.5
    temperature: 0.9
  evaluate:
    model: gpt-5.5
    temperature: 0.1
  synthesize:
    model: gpt-5.5
    temperature: 0.3

variables:
  problem: "How can a mid-size SaaS company reduce cloud infrastructure costs by 40% without degrading performance?"
